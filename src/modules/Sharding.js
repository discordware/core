class Sharding {
    constructor(options, logger, token, ) {
        this.options = options;
        this.token = token;
        this.logger = logger;
    }

    init() {

    }
}

module.exports = Sharding;