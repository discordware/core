class Sharding {
    constructor(options, token, logger) {
        this.options = options;
        this.token = token;
        this.logger = logger;
    }

    init() {

    }
}

module.exports = Sharding;